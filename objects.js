var playlist = {
  "John Mayer": "Gravity",
  "Dave Matthews": "So Damn Lucky"
};

function updatePlaylist(playlist, artist, song) {
  return Object.assign(playlist, {"Kings of Leon": "Sex on Fire"})
}